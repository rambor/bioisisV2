<div id="LeftMenu">
  <h3>FOUND:</h3>
  <% @ids.each do |x| %>
    <p><%= link_to x.bioisis_id, experiment_path(x.id), class:"bioisis-link" %></p>
    <p class="leftsearch"><%= truncate_words(x.title, 8) %></p>
  <% end %>
</div>